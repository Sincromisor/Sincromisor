[project]
name = "speech-extractor"
version = "2.0.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "sincro-config",
    "sincro-models",
    "numpy==1.26.4",
    "mediapipe==0.10.32",
    "pydantic>=2.11.7",
    "uvicorn>=0.35.0",
    "fastapi>=0.116.1",
    "websockets>=15.0.1",
    "setproctitle>=1.3.6",
]

[tool.uv.sources]
sincro-config = { workspace = true }
sincro-models = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["src/speech_extractor"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

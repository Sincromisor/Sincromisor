# 実装記録

## 履歴から確認した変更

実装コミットは `a85f2ee600ee4f6f4e415ef99faa9d66d78aa9eb`。今回の記録補完では本番コードを変更していない。

- `SincroFaceTracker` のMediaPipe呼び出しを共通処理へ集約し、内部推論時刻を単調増加へ補正した。
- 同一フレームの全画面・ROI・代替推論と、停止後の映像時刻の巻き戻りを扱い、スナップショットの映像時刻は維持した。
- 回帰試験と顔追跡の設計説明を追加・更新した。

## 確認結果と残事項

元コミットの本文には、対象14試験、`npm run check`、`npm run build`、タスク整合確認の成功が記録されている。これは履歴に記載された結果であり、今回の再実行結果ではない。実カメラでの手動確認は元コミット時点で未実行と記録されており、今回も実施していない。

今回の文書補完に対する確認は[評価記録](eval.md)に記載する。

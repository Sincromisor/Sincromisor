# Implementation Log: task-260705214907-full-normalized-pose-production-default

## Completion Summary

-

## Verification

-

## Not Run

-

## attempt 1

### 判断 / 逸脱

- `fullNormalizedPoseApplicationMode` の production default を `"upper_body"` に昇格した。`"off"` は削除せず、Debug Console 限定の明示 rollback mode として残した。
- `composerArmApplicationMode` / `composerTorsoShoulderApplicationMode` / `composerSemanticFingerApplicationMode` は削除していない。full application success frame では `VRMCharacterManager.update()` が既存どおり `ArmBoneController.update()` と `motionOrchestrator.update()` を skip し、unavailable / invalid / missing profile / result missing / vrm missing では staged rollback path を使う設計を維持した。
- 通常設定 UI、URL query、env、backend API、保存設定 contract は変更していない。Debug Console control は既存境界のまま、default 値だけ `DEFAULT_SINCRO_POSE_RETARGET_CONFIG` 経由で `upper_body` になる。
- 公開 WebRTC / backend 契約、DataChannel payload、server code は変更なし。

### ドキュメント同期

- `documents/design/frontend/character/motion.md`: full application が production default になったこと、`"off"` は staged rollback mode として残すこと、削除条件は cleanup inventory を正本にすることを同期。
- `documents/design/frontend/character/overview.md`: overview 側の full application stage も production default 表現に同期。
- `tasks/character-sincro-motion/task-260629225907-sincro-runtime-motion-ownership-map/artifacts/runtime-motion-ownership-map.md`: row 8 / 8.5 / 13 と cleanup status を default `"upper_body"` 前提へ更新し、head / neck / leg / expression / root position は非対象のまま記録。
- `tasks/character-sincro-motion/task-260705004418-production-motion-rollback-and-cleanup/artifacts/production-motion-cleanup-inventory.md`: full application 常時有効化候補を「default 昇格後の継続確認」へ更新し、staged rollback flag 削除条件を残した。
- `tasks/character-sincro-motion/task-260705004418-production-motion-rollback-and-cleanup/artifacts/production-motion-rollback-runbook.md`: full finalPose stage の rollback 手順を production default `"upper_body"` 後の復旧手順として同期。

### 検証

- focused harness: `npm run test -- src/character/vrmCharacter/__tests__/armBoneController.test.ts src/character/runtime/__tests__/sincroVrmPoseComposerDryRun.test.ts src/character/motionEvaluation/__tests__/motionComposerComparisonMetrics.test.ts src/pages/motionDebug/__tests__/motionDebugPhase6Snapshots.test.ts src/features/debug/model/__tests__/debugConsoleSincroMotionControls.test.ts src/character/motionEvaluation/__tests__/motionQaRegression.test.ts src/pages/motionDebug/__tests__/motionDebugViewerModel.test.ts src/features/gaze/trackingRuntime/__tests__/trackerRuntimeDegradationPolicy.test.ts src/features/gaze/trackingRuntime/__tests__/trackerRuntime.test.ts` -> PASS, 9 files / 105 tests.
- P0 replay fixture: `motionQaRegression.test.ts` と `motionDebugViewerModel.test.ts` の synthetic fixture / QA regression harness で代替確認。captured replay log はこの worktree に無いため実データ replay は未実行。
- camera degradation / recovery: `trackerRuntimeDegradationPolicy.test.ts` と `trackerRuntime.test.ts` で degradation order、reverse recovery、face-only recovery gating、Pose / Face ROI / Hand resume を確認。実カメラ session は未実行。
- browser smoke: `npm run dev -- --host 127.0.0.1 --port 5177` + `playwright-cli`。
    - `/motion-debug/?vrm=/characters/default.vrm`: title `Sincro Motion Debug`, canvas `2`, `window.__SINCRO_MOTION_DEBUG__` あり, console error `0`。
    - `/motion-debug/?vrm=/characters/aoi-1.0.7.vrm`: title `Sincro Motion Debug`, canvas `2`, `window.__SINCRO_MOTION_DEBUG__` あり, console error `0`。
    - `/simple-vrm/`: RTC config endpoint を contract-compatible payload で mock。canvas `1`, talk mode select に `chat` / `sincro`, rollback controls あり, `sincroPoseFullNormalizedApplication` default value `upper_body`, console error `0`。
    - UI switch: talk mode を `sincro`、rollback controls を `both` / `composer` / `off` / `off` に変更し DOM value 反映と console error `0` を確認。
- `npm run gate` -> PASS。
    - lint: `cd sincromisor-frontend && npm run check` PASS。
    - build: `cd sincromisor-frontend && npm run build` PASS。
    - test: `cd sincromisor-frontend && npm run test` PASS, 65 files / 500 tests。

### 未実行 / 残リスク

- 実カメラ session と実 backend RTC 接続は未実行。browser smoke では backend absence を避けるため `/api/v1/RTCSignalingServer/config.json` のみ mock した。
- captured P0 replay log / real VRM visual artifact はこの worktree に無いため未実行。synthetic P0 fixture harness、focused tests、複数 VRM browser smoke で代替した。
- staged rollback flags は意図どおり残置しており、後続の rollback 削除 task までは Debug Console control / summary metadata が残る。

### TypeScript production comment audit

| path                                                                                          | symbol or decision                                                      | kind                                          | current comment                                                                                                                         | decision | required maintenance knowledge                                                                                                                                                                                                                                       | action                                                                                                                                                   | reviewer note                                                                                           |
| --------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- | --------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| `sincromisor-frontend/src/character/retargeting/sincroPoseRetargetTypes.ts`                   | `FullNormalizedPoseApplicationMode`                                     | public export / developer rollback flag       | stale: `"off"` を既定値として説明していた                                                                                               | rewrite  | `"upper_body"` が production default。`"off"` は通常設定 contract ではなく Debug Console 限定 rollback mode。削除条件は P0 replay、camera degradation / recovery、chat / sincro mode 切替、複数 VRM の継続 PASS。                                                    | TSDoc を rewrite。通常設定 UI / 保存設定 contract へ広げない境界、削除条件、rollback mode を明記。                                                       | union は `"off" \| "upper_body"` のまま、default だけ `upper_body`。                                    |
| `sincromisor-frontend/src/character/retargeting/sincroPoseRetargetTypes.ts`                   | `SincroPoseRetargetConfig.fullNormalizedPoseApplicationMode`            | public config field / lifecycle fallback      | stale: field comment が `"off"` default と「常時有効化 task まで削除しない」を説明していた                                              | rewrite  | default `"upper_body"` success frame は `setNormalizedPose(finalPose)` を 1 回呼び、arm / torso / shoulder direct writer を skip。unavailable / invalid / missing profile では stale result を使わず identity clear 後に staged rollback。通常設定 contract 非公開。 | field TSDoc を rewrite。stale comment を更新し、fallback 条件と副作用を明記。                                                                            | `VRMCharacterManager.update()` の success / rollback tests と照合。                                     |
| `sincromisor-frontend/src/character/retargeting/sincroPoseRetargetTypes.ts`                   | `DEFAULT_SINCRO_POSE_RETARGET_CONFIG.fullNormalizedPoseApplicationMode` | domain-significant const / production default | config object 自体に個別コメントなし。近傍 type / field TSDoc が契約を説明。                                                            | keep     | production default は `upper_body`。Debug Console default snapshot と manager private field はこの const から読む。                                                                                                                                                  | 値を `"upper_body"` へ変更。近傍 TSDoc で契約を覆うため const への逐語コメントは追加しない。                                                             | `armBoneController.test.ts` と `debugConsoleSincroMotionControls.test.ts` に default assertion を追加。 |
| `sincromisor-frontend/src/character/vrmCharacter/vrmCharacterManager.ts`                      | staged rollback 残置判断                                                | lifecycle / fallback                          | class TSDoc と helper TSDoc が full application 失敗時は staged rollback path に戻すことを説明済み                                      | keep     | default 昇格後も `composerArmApplicationMode` / `composerTorsoShoulderApplicationMode` は削除しない。full unavailable 時だけ direct writer が復旧 path になる。                                                                                                      | production code は変更せず、docs / ownership map / cleanup inventory に残置理由を同期。                                                                  | full success test で direct writer skip、rollback tests で direct writer call を確認。                  |
| `sincromisor-frontend/src/character/vrmCharacter/vrmCharacterManager.ts`                      | identity clear fallback                                                 | lifecycle / fallback                          | `setSincroPoseRetargetConfig()` 内 comment と `applyFullNormalizedPoseApplication()` TSDoc が previous full-owned pose clear を説明済み | keep     | rollback frame では full-owned upper body / finger pose の残留を避けるため staged writer 前に identity pose を 1 回適用する。                                                                                                                                        | 実コードコメントは維持。default 変更で stale にならないことを確認。                                                                                      | `clears previous full application before staged rollback writers run` と mode off test を確認。         |
| `sincromisor-frontend/src/character/vrmCharacter/vrmCharacterManager.ts`                      | non-target controller 維持                                              | ownership boundary                            | class / helper TSDoc と design docs が head / neck / leg / expression / root position 非対象を説明済み                                  | keep     | full upper-body finalPose は head / neck / leg / expression / root position を所有しない。Face / Eye / Mouth / Emotion / Leg は従来 controller 更新を続ける。                                                                                                        | production code は変更せず、motion.md / overview.md / ownership map に default 昇格後も非対象であることを同期。                                          | full success test で `legBoneController.update()` と `vrm.update()` が呼ばれることを確認。              |
| `sincromisor-frontend/src/features/debug/model/debugConsoleSnapshot.ts`                       | Debug Console default snapshot boundary                                 | boundary / developer flag                     | default snapshot は `DEFAULT_SINCRO_POSE_RETARGET_CONFIG` から値を読む構造で、個別 field comment はなし                                 | keep     | full flag は Debug Console developer control に限定し、保存設定 contract ではない。default snapshot は config default 変更を反映する。                                                                                                                               | production code コメント追加は省略。`debugConsoleSincroMotionControls.test.ts` に default `upper_body` assertion を追加し、docs に通常設定非公開を記録。 | 通常設定 UI / URL query / env / backend API への露出なし。                                              |
| `sincromisor-frontend/src/features/debug/react/panels/sincroPoseRetargetComposerControls.tsx` | Debug Console 限定境界                                                  | public component / developer control          | component 内に boundary comment はなく、既存 UI が Debug Console panel 配下で完結                                                       | keep     | developer control として残す。通常設定 UI には公開しない。                                                                                                                                                                                                           | production code 変更なし。motion.md と cleanup inventory に境界を同期。                                                                                  | `/simple-vrm/` smoke で rollback controls と default `upper_body` を確認。                              |
| `sincromisor-frontend/src/character/vrmCharacter/__tests__/armBoneController.test.ts`         | default / success / rollback tests                                      | test                                          | 対象外                                                                                                                                  | keep     | 受け入れ条件を regression test で固定する。                                                                                                                                                                                                                          | default assertion を追加。test file のため production comment audit 対象外。                                                                             | focused harness と gate で PASS。                                                                       |

TODO 追加なし。stale comment は `FullNormalizedPoseApplicationMode` と `SincroPoseRetargetConfig.fullNormalizedPoseApplicationMode` の TSDoc を rewrite して解消した。module TSDoc への集約は行っていない。

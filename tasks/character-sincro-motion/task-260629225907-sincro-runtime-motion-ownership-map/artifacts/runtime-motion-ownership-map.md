# Runtime Motion Ownership Map

## Scope

この artifact は、現行 `simple-vrm` / `sincro` runtime の `VRMCharacterManager.update()` が、どの順序で bone / expression / root position を更新しているかを記録する。
対象は production runtime の書き込み順序であり、`motion-debug` の replay / final-pose-playback 専用経路は主対象にしない。

現時点では本番書き込み順序を変更しない。`VrmPoseComposer` は developer-only path に留まり、以下の分類は後続移行タスクが二重書き込みを避けるための判断材料である。

分類:

- `move-to-composer`: 将来 `VrmPoseComposer` の final pose 所有へ移す候補。
- `keep-controller-owned`: 現行 controller / retargeter 所有を維持する候補。
- `needs-decision`: 所有境界を追加判断してから移行する候補。

## Runtime Order Map

| 順序 | 書き手                                                                          | 対象 bone / expression / root position                                                                                                                                                                             | 入力 snapshot                                                                                                                  | 既存 fallback                                                                                                                                                                                                                         | `sincro` での有効条件                                                                                                                                                                        | `chat` での有効条件                                                                                                                 | 分類                    |
| ---- | ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ----------------------- |
| 1    | `SincroFaceRetargeter.retarget()`                                               | 直接の VRM 書き込みなし。`SincroFaceRetargetFrame.head` と `expressions` を生成し、内部 smoothing state を更新する。                                                                                               | `CharacterBehaviorSnapshot.faceMotion`、`nowMs`                                                                                | tracking disabled / lost / low confidence では neutral head / neutral expressions へ smoothing する。                                                                                                                                 | `allowFaceRetarget=true` のため毎 frame 呼ばれる。後段 controller が使うのは `faceMotion.trackingEnabled` かつ retarget frame が渡る場合。                                                   | 毎 frame 呼ばれるが `allowFaceRetarget=false` のため後段 controller は chat fallback を使う。                                       | `keep-controller-owned` |
| 2    | `SincroPoseRetargeter.retarget()`                                               | 直接の VRM 書き込みなし。`SincroPoseRetargetFrame.upperBody`、左右 arm Euler / IK quaternion、wrist roll を生成し、内部 smoothing state を更新する。                                                               | `CharacterBehaviorSnapshot.poseMotion`、`motionPolicy.allowPoseRetarget`、`nowMs`                                              | pose disabled / face-only / lost / low confidence では `NEUTRAL_POSE_FRAME` に fallback reason を付けて return-to-neutral smoothing する。腕単位では not tracked / low confidence で neutral arm、IK 不可で feature-only arm に戻る。 | `poseMotion.trackingEnabled && !poseMotion.degradedToFaceOnly` の場合だけ実 pose snapshot を読む。それ以外は `pose_retarget_disabled` snapshot を渡す。                                      | `allowPoseRetarget=false` のため常に `pose_retarget_disabled` snapshot を渡す。                                                     | `keep-controller-owned` |
| 3    | `DebugConsoleManager.updateSincroPoseRetargetFrame()`                           | VRM 書き込みなし。Debug Console の pose retarget 表示 state。                                                                                                                                                      | `SincroPoseRetargetFrame`                                                                                                      | なし。表示用に最新 frame を保持するだけ。                                                                                                                                                                                             | `sincroPose` の内容に追従。                                                                                                                                                                  | `pose_retarget_disabled` frame を表示する。                                                                                         | `keep-controller-owned` |
| 4    | `HeadBoneController.update()`                                                   | `upperChest` / `neck` / `head` rotation。fallback では `neck` 優先、欠損時 `head` / `upperChest` / `chest` / `spine` の最初の node の position / rotation / scale。                                                | `CharacterBehaviorSnapshot`、`SincroFaceRetargetFrame`、`VRMCamera`、`CharacterGaze`                                           | `sincro` face retarget が使えない場合は gaze target、gaze 無効時は camera direction。`neck/head/upperChest` が無い場合は fallback control node に合算 rotation を書く。AI speech gesture は chat fallback 側だけ加算される。          | `allowFaceRetarget && faceMotion.trackingEnabled && sincroFace` で retargeted head を優先。face lost 時は gaze/camera fallback に戻るが `allowGazeMotion=false` なので主に camera fallback。 | `allowFaceRetarget=false`、`allowGazeMotion=true`。gaze detected / stale state または camera fallback と AI speech gesture を書く。 | `needs-decision`        |
| 5    | `EyeBehaviorController.update()`                                                | look expressions (`lookLeft` / `lookRight` / `lookUp` / `lookDown`)、blink expressions、fallback `leftEye` / `rightEye` rotation。                                                                                 | `CharacterBehaviorSnapshot`、`SincroFaceRetargetFrame`                                                                         | look expression 欠損時は eye bone rotation。blink expression 欠損時は eye bone blink fallback。通常時は gaze / AI speech / thinking aversion target を smoothing する。                                                               | `allowFaceRetarget && faceMotion.trackingEnabled && sincroFace` で retargeted look / blink を優先。expression が無い軸だけ eye bone fallback。                                               | `allowFaceRetarget=false`。gaze / AI speech / thinking aversion 由来の eye target と通常 blink を使う。                             | `needs-decision`        |
| 6    | `FaceMorphController.update()`                                                  | mouth expressions (`aa` / `ih` / `ou` / `oh` / `ee`)。                                                                                                                                                             | `CharacterBehaviorSnapshot.aiSpeech`、`SincroFaceRetargetFrame`                                                                | `sincro` retarget が有効な場合は mora state を reset し retargeted mouth を clamp。chat lip sync 無効 / speaking なし / mora 欠損時は mouth presets を reset。                                                                        | `allowFaceRetarget && faceMotion.trackingEnabled && sincroFace` で user mouth retarget を優先。                                                                                              | `allowAiLipSync && aiSpeech.isSpeaking && currentMoraId` の場合だけ telop vowel lip sync。neutral transition 中は reset。           | `keep-controller-owned` |
| 7    | `FaceEmotionController.update()`                                                | emotion expressions (`relaxed` / `happy` / `sad` / `angry` / `surprised`) と neutral reset。初期化時に mouth viseme と重複する emotion binds を削除する副作用がある。                                              | `CharacterBehaviorSnapshot.aiSpeech.lastTextMessage`、`expression_code`、`nowMs`                                               | `allowAiEmotion=false` では active emotion を破棄し neutral reset。expression_code 欠損の speech では neutral を即時適用。存在しない preset は `setValue()` 先としては維持しつつログに残す。                                          | `allowAiEmotion=false` のため emotion animation は無効化される。                                                                                                                             | neutral transition 後、system text message の `expression_code` を短時間の emotion preset animation に変換する。                    | `keep-controller-owned` |
| 8    | `ArmBoneController.update()` + `applyArmBoneRotations()` / `applyArmHandPose()` | `leftUpperArm` / `rightUpperArm` / `leftLowerArm` / `rightLowerArm` rotation または IK quaternion、`leftHand` / `rightHand` rotation、`leftThumbProximal` / `rightThumbProximal` と子 bone の recursive rotation。 | `motionElapsedSeconds`、`CharacterBehaviorSnapshot.aiSpeech`、`SincroPoseRetargetFrame.leftArm/rightArm`                       | 腕 IK active の side は speech gesture を 0 にし idle scale を 0.22 に下げる。`world_3d_ik` では upper/lower arm quaternion を直接適用し、不可時は Euler feature + idle + speech gesture に戻る。hand / thumb 欠損時は skip。         | pose retarget が有効な腕は IK / feature retarget を優先し、同じ腕の speech gesture を抑制する。idle は低倍率で残る。                                                                         | pose は disabled frame なので idle + AI speech gesture + fixed hand/thumb pose が主。                                               | `move-to-composer`      |
| 9    | `LegBoneController.update()`                                                    | `leftUpperLeg` / `leftLowerLeg` / `rightUpperLeg` / `rightLowerLeg` / `leftFoot` / `rightFoot` rotation。                                                                                                          | `motionElapsedSeconds`                                                                                                         | 欠損 bone は skip。右脚/足だけ idle sway を加え、左脚は固定寄りの default pose。                                                                                                                                                      | 常時有効。`idleMotionScale` や pose retarget の影響は受けない。                                                                                                                              | 常時有効。`idleMotionScale` や AI speech state の影響は受けない。                                                                   | `keep-controller-owned` |
| 10   | `vrm.update(deltaSeconds)`                                                      | three-vrm 内部更新。expression manager / spring bone 等の VRM runtime state。                                                                                                                                      | `deltaSeconds` と直前までに controller が書いた expression / bone state                                                        | three-vrm 側の標準挙動。project-local fallback なし。                                                                                                                                                                                 | 常時有効。                                                                                                                                                                                   | 常時有効。                                                                                                                          | `keep-controller-owned` |
| 11   | `VRMCharacterManager.update()` root position block                              | `hips` normalized bone の `position`。`defaultPosition + characterPosition` を copy する。                                                                                                                         | `defaultPosition`、`characterPosition`                                                                                         | `rootBone` が無い場合は skip。`defaultPosition` は load 後に `hips` position から capture。                                                                                                                                           | 常時有効。                                                                                                                                                                                   | 常時有効。                                                                                                                          | `keep-controller-owned` |
| 12   | `CharacterMotionOrchestrator.update()` root stabilization                       | optional `hips` の `position` と `rotation`。`position` は同じ `hipsBasePosition` を再 copy し、`rotation` は load 時 baseRotation へ戻す。                                                                        | `CharacterBehaviorSnapshot`、`hipsBasePosition`、`SincroPoseRetargetFrame`、`motionElapsedSeconds`                             | optional `hips` が capture できない場合は skip。root position は `VRMCharacterManager` と同じ値を使う。                                                                                                                               | 常時有効。ただし `idleMotionScale` は `sincro` で低め、listening / AI speech gesture は policy で無効。                                                                                      | 常時有効。listening / AI speech gesture / thinking aversion が policy と state に応じて torso helper へ渡る。                       | `keep-controller-owned` |
| 13   | `CharacterMotionTorsoApplier` via `CharacterMotionOrchestrator.update()`        | `spine` / `chest` / `upperChest` / `leftShoulder` / `rightShoulder` rotation。                                                                                                                                     | `CharacterBehaviorSnapshot`、`SincroPoseRetargetFrame.upperBody`、idle breath/listening/AI speech motion frame、base rotations | optional bone 欠損時は skip。pose retarget 値は各 axis に additive。`upperChest` は `pose.upperBody.chest.z * 0.45` を使う。                                                                                                          | `sincro` では `idleMotionScale=0.42` または neutral transition `0.25`。pose upperBody が有効なら torso / shoulder に additive。AI speech / listening は policy で無効。                      | `pose_retarget_disabled` なので pose additive は neutral。idle / listening / AI speech beat / backchannel nod が主。                | `move-to-composer`      |

## Needs Decision

| 対象                                                       | 決めるべき所有境界                                                                                                                                         | 衝突する既存書き手                                                                                                             | 後続タスクでの判断先                                                                                                                                                               |
| ---------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `HeadBoneController` の `upperChest` / `neck` / `head`     | face retarget head pose を composer final pose に含めるか、head/neck は controller 所有で残すか。特に `upperChest` を head と torso のどちらが所有するか。 | `HeadBoneController` が `upperChest` を先に書き、`CharacterMotionTorsoApplier` が同 frame 後段で `upperChest` を上書きしうる。 | `production-sincro-motion-pipeline-state-contract` で live state の置き場所を決め、`torso-shoulder-composer-ownership-migration-plan` で torso / shoulder と同時に境界を確定する。 |
| `EyeBehaviorController` の `leftEye` / `rightEye` fallback | look / blink expression は expression controller 所有で残しつつ、eye bone fallback だけ final pose に移すか。                                              | 現行では eye bone の既存書き手は `EyeBehaviorController` のみだが、将来 composer が eye bone を所有すると二重書き込みになる。  | head / neck / expression の所有境界を扱う後続 task で、expression と bone fallback を分離して判断する。                                                                            |
| `LegBoneController` の leg / foot idle pose                | leg / foot は current idle controller 所有を維持するか、final pose 全面移行時に composer の owned bones に含めるか。                                       | 現行では `LegBoneController` のみ。将来 `vrm.humanoid.setNormalizedPose(finalPose)` を全面適用する場合に衝突する。             | final pose replay / full `setNormalizedPose` 移行 task で、leg を composer 非対象として明示するか leg layer を追加するか判断する。                                                 |

## Follow-up Notes

- `SincroFaceRetargeter` と `SincroPoseRetargeter` は VRM bone / expression の直接書き手ではなく、controller が読む retarget frame の所有者である。composer 移行後も raw tracker snapshot と VRM final pose の間の変換境界として残す候補。
- `FaceMorphController` と `FaceEmotionController` は expression owner として維持する候補。`VrmPoseComposer` v1 は expression を対象にしない。
- `VRMCharacterManager.update()` の root position block と `CharacterMotionOrchestrator.stabilizeHips()` は同じ `hipsBasePosition` を書くため、現状は値の衝突ではなく順序上の重複である。root position は `VrmPoseComposer` の normalized local pose 対象外として扱う。
- 本調査では TypeScript production code のコメント修正は行わなかった。

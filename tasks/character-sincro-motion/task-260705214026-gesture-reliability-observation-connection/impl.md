# Implementation Log: task-260705214026-gesture-reliability-observation-connection

## Completion Summary

-

## Verification

-

## Not Run

-

## attempt 1

### 判断 / 申し送り対応

- review は `APPROVED` のため実装を進めた。Critical / High 指摘はなし。
- `GestureReliability.components.temporal` は review.md の申し送り通り未入力 component として `score: 0` / `reasonCodes: ["no_observation"]` にした。安定性は `stableDurationMs` に閉じ、temporal component と二重定義しない。
- `stableDurationMs` は前回 `ReliabilityMap.gesture` だけで計算できるよう、gesture reliability に normalized `side` と `lastUpdatedAtMs` を optional 追加した。MediaPipe raw handedness object ではなく、`GestureIntentObservation` の left/right key 由来の normalized side である。
- `SincroMotionObserveOnlyPipeline.updateGesture()` は latest gesture observation と summary だけを保存し、Gesture callback 単独では Pose / canonical / temporal / intent を再計算しないようにした。次回 Pose / reliability 更新時に `createPoseReliabilityMap()` へ latest gesture を渡す。
- MotionIntent gate は ReliabilityMap がある場合 `ReliabilityMap.gesture.finalWeight`、該当 hand part、finger part を使う。ReliabilityMap 欠損の legacy / test 入力だけ hand side confidence fallback を維持した。
- debug / replay viewer は既存 reliability layer が saved `frame.reliability` を parse して表示する境界を維持し、`ReliabilityMap.gesture` の `source` / `finalWeight` / `confidence` / `stableDurationMs` / warnings が見えることを test に追加した。raw category list / raw handedness object は reliability layer に保存しない。
- `npm run check` が別タスク `task-260705214018-mediapipe-raw-result-replay` の `impl.md` / `eval.md` Markdown formatting で失敗したため、実装 worktree 側だけ Prettier の要求する table/list indentation を適用した。実装仕様とは無関係だが、`npm run gate` を通す前提として必要だった。

### ドキュメント同期

- `documents/design/frontend/character/motion.md` を同期し、Gesture reliability の入力、source 判定、unknown label と semantic intent の分離、observe-only cadence 境界、MotionIntent gate の ReliabilityMap 利用を反映した。
- `documents/design/frontend/character/tracking.md` を同期し、`ReliabilityMap.gesture` の component、stability duration、debug / replay reliability layer 表示、raw category / handedness 非保存境界を反映した。
- 公開 WebRTC / backend 契約、DataChannel payload、server code は変更していないため、contract docs の同期は不要。

### TypeScript production comment audit

| path                                                                                  | symbol or decision                            | kind                                 | current comment                                                                        | decision         | required maintenance knowledge                                                                                                                                                                                                                                                                                             | action                                                                                                         | reviewer note                                                                                                                       |
| ------------------------------------------------------------------------------------- | --------------------------------------------- | ------------------------------------ | -------------------------------------------------------------------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| `sincromisor-frontend/src/character/reliability/gestureReliabilityEstimator.ts`       | `createGestureReliability`                    | public export / heuristic / boundary | 新規 file。既存コメントなし。                                                          | add              | 入力は normalized gesture / Hand snapshot / previous gesture reliability / CameraQualityScore / caller media time に限定する。raw category list / handedness object は読まない。unknown label は source gesture だが semantic intent にはしない。失敗は throw せず neutral / low weight reliability に落とす。副作用なし。 | public export TSDoc を追加。                                                                                   | evaluator は raw field 非参照、source gesture と unknown label 分離、parseable ReliabilityMap を確認する。                          |
| `sincromisor-frontend/src/character/reliability/gestureReliabilityEstimator.ts`       | stability duration side + label rule          | heuristic / threshold                | 新規実装。                                                                             | add              | 同じ normalized side + label、confidence `>= 0.70`、media time 正方向だけ加算する。dt は `0..1000ms` に clamp し、`stableDurationMs < 160` は `finalWeight <= 0.5` に cap する。誤調整すると gesture flicker が MotionIntent に流れる。                                                                                    | constants と public TSDoc で threshold / failure mode を説明。                                                 | low confidence、side change、media time regression、first frame cap の test を追加。                                                |
| `sincromisor-frontend/src/character/reliability/reliabilityMap.ts`                    | `GestureReliability.side` / `lastUpdatedAtMs` | schema / parser                      | GestureReliability は既存で comment なし。                                             | add              | side は `GestureIntentObservation` の left/right key 由来の normalized side。`lastUpdatedAtMs` は stable duration 用の media time で raw handedness ではない。旧 log 互換のため optional。schemaVersion は維持。                                                                                                           | optional schema fields を追加。個別 TSDoc は既存 type 群に合わせ省略し、docs と estimator TSDoc へ境界を記録。 | parseReliabilityMap の後方互換、raw handedness 非保存を確認する。                                                                   |
| `sincromisor-frontend/src/character/reliability/poseReliabilityEstimator.ts`          | `createPoseReliabilityMap` gesture input      | public export / boundary             | 既存 public function は comment なし。周辺 design doc と tests が契約を持つ。          | rewrite/add docs | optional `gesture` が未指定なら従来 placeholder。指定時だけ gesture estimator を使い、previous は `previous.reliability?.gesture` を渡す。Pose / Hand / Face reliability の既存計算は変更しない。                                                                                                                          | 実装を分岐追加。docs と focused test を追加。function TSDoc は既存 file style に合わせ省略。                   | placeholder 維持と optional gesture 接続の両方を test で確認する。                                                                  |
| `sincromisor-frontend/src/character/runtime/sincroMotionObserveOnlyPipeline.ts`       | `updateGesture()` ordering                    | lifecycle / boundary                 | 既存コメントは `ReliabilityMap.gesture` placeholder 維持と書いており stale。           | rewrite          | Gesture callback は Pose cadence と異なるため latest observation / summary だけ保存し、Pose / canonical / temporal / intent は再計算しない。次回 reliability 更新で同じ observation を使う。VRM 適用なし。                                                                                                                 | class TSDoc と method TSDoc を rewrite。実装から downstream call を削除。                                      | Gesture callback 単独で reliability が進まないこと、次 Pose で reliability gesture が source gesture になることを test で確認する。 |
| `sincromisor-frontend/src/character/runtime/sincroMotionObserveOnlyPipelineTypes.ts`  | raw label 非保存境界                          | boundary                             | 既存コメントは ReliabilityMap placeholder と書いており stale。                         | rewrite          | pipeline input の `gesture` は normalized observation だけ。raw category list / handedness object は渡さず、reliability には side / label / confidence / stableDurationMs を保存する。                                                                                                                                     | input type TSDoc を rewrite。                                                                                  | raw object を input 型に追加していないことを確認する。                                                                              |
| `sincromisor-frontend/src/character/motionIntent/motionIntentCandidateDetectors.ts`   | `passesGestureGate` ReliabilityMap priority   | heuristic / fallback                 | module comment は confidence gate と tests 確認のみで、ReliabilityMap 優先条件が不足。 | rewrite          | ReliabilityMap がある場合は `gesture.finalWeight` と hand / finger reliability を正本にする。ReliabilityMap 欠損時だけ raw gesture confidence + hand confidence fallback を使う。誤調整すると semantic intent flicker または過抑制になる。                                                                                 | module TSDoc と gate logic を更新。                                                                            | low gesture finalWeight、low hand reliability、legacy missing map fallback を test で確認する。                                     |
| `sincromisor-frontend/src/pages/motionDebug/__tests__/motionDebugViewerModel.test.ts` | reliability layer raw 非保存境界              | debug / replay boundary              | production code の viewer layer は ReliabilityMap 全体を表示。                         | keep / add tests | viewer は saved `frame.reliability` を正本にし、raw Gesture category list / handedness object は reliability layer へ混ぜない。                                                                                                                                                                                            | production code は既存境界を維持し、test で gesture fields と raw 非混入を確認。                               | evaluator は saved reliability に gesture fields が表示されることを確認する。                                                       |

TODO は追加していない。stale comment は observe-only pipeline と input type の placeholder 記述を rewrite した。module TSDoc への一括集約はせず、export / decision 単位で必要な保守知識を分けて記録した。

### Verification

- `npm run test -- gestureReliabilityEstimator poseReliabilityEstimator reliabilityMap sincroMotionObserveOnlyPipeline motionIntentEstimator motionDebugViewerModel`（cwd: implementation worktree `sincromisor-frontend`）: PASS。6 files / 86 tests。
- `npm run check`（cwd: implementation worktree `sincromisor-frontend`）: PASS。
- `npm run build`（cwd: implementation worktree `sincromisor-frontend`）: PASS。Vite の既存 chunk size warning は出たが build は成功。
- `npm run test`（cwd: implementation worktree `sincromisor-frontend`）: PASS。65 files / 497 tests。
- `npm run gate`（cwd: implementation worktree root）: PASS。lint / build / test 全 step PASS。
- `git diff --check`（cwd: implementation worktree root）: PASS。

### 残リスク

- motion-debug live runtime は現状 Gesture optional pass を有効化していないため、motion-debug recording が新規に gesture reliability を生成するのは saved `frame.reliability` を持つ経路または production observe-only 由来の reliability に限られる。replay viewer は saved reliability を正本に表示する。
- `ReliabilityMap.gesture.side` / `lastUpdatedAtMs` は optional 追加で旧 log 互換を維持する。schemaVersion は維持した。

## attempt 2

### 判断 / 評価 FAIL 対応

- 評価 FAIL は挙動・テストではなく comment audit と実コードコメントの不一致だったため、ロジックは変更せず production code の保守コメントを追加した。
- `gestureReliabilityEstimator.ts` に threshold 値 `0.70` / `160ms` / `1000ms`、reset 条件、dt clamp、`finalWeight` cap、誤調整時の failure mode、MediaPipe raw category / handedness object を読まない境界を実コードコメントとして追加した。
- `GestureReliabilityInput` に TSDoc を追加し、normalized side key 由来、raw object 非受理、旧 log 互換の `previous`、stable duration 用 `mediaTimeMs`、入力欠損時に neutral reliability を返す失敗条件を明記した。
- `GestureReliability.side` / `label` / `lastUpdatedAtMs` に field-level TSDoc を追加し、raw handedness ではないこと、raw category list ではないこと、旧 log 互換で optional であること、stable duration 用 media time であることを明記した。
- `PoseReliabilityEstimatorInput` / `PoseReliabilityEstimatorInput.gesture` / `createPoseReliabilityMap()` に TSDoc を追加し、gesture 未指定時は neutral placeholder を維持すること、指定時だけ normalized gesture を estimator へ渡すこと、raw MediaPipe Gesture result を読まないこと、`previous.reliability?.gesture.lastUpdatedAtMs` と caller 指定 `mediaTimeMs` で stability を更新することを明記した。

### TypeScript production comment audit

| path                                                                            | symbol or decision                            | kind                                  | current comment                                                                                        | decision | required maintenance knowledge                                                                                                                                                                                                                                                                     | action                                      | reviewer note                                                                                     |
| ------------------------------------------------------------------------------- | --------------------------------------------- | ------------------------------------- | ------------------------------------------------------------------------------------------------------ | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| `sincromisor-frontend/src/character/reliability/gestureReliabilityEstimator.ts` | `GestureReliabilityInput`                     | public export / boundary type         | attempt 1 では comment なし。                                                                          | add      | `gesture` は `SincroGestureMotionSnapshot` から抽出済みの normalized side key / label / confidence だけ。raw category list、raw handedness object、landmark、crop/image object は渡さない。`previous` は旧 log 互換で optional。`mediaTimeMs` は stable duration と `lastUpdatedAtMs` の時刻基準。 | type TSDoc と field-level TSDoc を追加。    | evaluator は raw object 非受理境界、旧 log 互換、placeholder 条件を実コードコメントで確認できる。 |
| `sincromisor-frontend/src/character/reliability/gestureReliabilityEstimator.ts` | gesture stability thresholds                  | heuristic / threshold                 | attempt 1 では constants の値・reset 条件・failure mode が TSDoc に無かった。                          | add      | confidence `< 0.70`、label change、side missing/change、previous timestamp missing、media time regression で reset。positive dt は `1000ms` clamp。`stableDurationMs < 160` は `finalWeight <= 0.5` cap。下げすぎると semantic intent flicker、上げすぎると valid gesture が抑制される。           | constants 直前に heuristic comment を追加。 | threshold 値と failure mode が production code 上で読める。                                       |
| `sincromisor-frontend/src/character/reliability/gestureReliabilityEstimator.ts` | `createGestureReliability`                    | public export / boundary / heuristic  | attempt 1 TSDoc は raw object 非参照と unknown label 分離だけ。threshold 値や placeholder 条件が不足。 | rewrite  | Gesture skip / lost では `source: "neutral"` placeholder。valid observation は unknown label でも `source: "gesture"`。stable duration は same normalized side + label、confidence `>= 0.70`、dt `0..1000ms`。160ms 未満は `finalWeight <= 0.5`。副作用なし。                                      | public export TSDoc を拡張。                | acceptance の threshold / reset / cap / raw 非保存境界と一致。                                    |
| `sincromisor-frontend/src/character/reliability/reliabilityMap.ts`              | `GestureReliability.side`                     | schema / field contract               | attempt 1 では field comment なし。                                                                    | add      | side は `GestureIntentObservation.left/right` key 由来の normalized side。MediaPipe raw handedness object や mirror UI の左右ではない。旧 log / placeholder では欠損可能。                                                                                                                         | field-level TSDoc を追加。                  | raw handedness 非保存と旧 log 互換を実コードで確認できる。                                        |
| `sincromisor-frontend/src/character/reliability/reliabilityMap.ts`              | `GestureReliability.label`                    | schema / field contract               | attempt 1 では field comment なし。                                                                    | add      | label は Gesture Recognizer top label の説明用 field。unknown label も保持するが MotionIntent enum ではない。raw category list は raw replay slot の責務。                                                                                                                                         | field-level TSDoc を追加。                  | semantic intent 昇格と reliability 保存の分離を確認できる。                                       |
| `sincromisor-frontend/src/character/reliability/reliabilityMap.ts`              | `GestureReliability.lastUpdatedAtMs`          | schema / temporal contract            | attempt 1 では field comment なし。                                                                    | add      | caller 指定 `mediaTimeMs` を保存する stable duration 用 timestamp。runtime clock ではない。旧 log 互換で optional、欠損時は次 frame で stability reset。                                                                                                                                           | field-level TSDoc を追加。                  | media time 用途と optional 互換が実コードで読める。                                               |
| `sincromisor-frontend/src/character/reliability/poseReliabilityTypes.ts`        | `PoseReliabilityEstimatorInput` / `gesture`   | public boundary type / field contract | attempt 1 では type comment なし。                                                                     | add      | `gesture` は normalized observation だけを受け、未指定時は `ReliabilityMap.gesture` neutral placeholder を維持する。side は raw handedness ではなく left/right key 由来。旧 log replay で保存されていない Gesture 観測を捏造しない。                                                               | type TSDoc と field-level TSDoc を追加。    | placeholder 維持条件と raw 非受理境界を確認できる。                                               |
| `sincromisor-frontend/src/character/reliability/poseReliabilityEstimator.ts`    | `createPoseReliabilityMap()` gesture boundary | public export / boundary              | attempt 1 では function comment なし。                                                                 | add      | `input.gesture` 未指定時は neutral placeholder。指定時だけ normalized side / label / confidence を gesture estimator へ渡し、`previous.reliability?.gesture.lastUpdatedAtMs` と caller `mediaTimeMs` で stable duration を更新する。raw Gesture result は読まない。                                | public function TSDoc を追加。              | acceptance の optional input と placeholder 維持条件を production code 上で確認できる。           |

TODO は追加していない。attempt 1 の audit にあった「constants と public TSDoc で説明」は attempt 2 の実コードコメント追加で実態と一致した。

### Verification

- `npm run test -- gestureReliabilityEstimator poseReliabilityEstimator reliabilityMap`（cwd: implementation worktree `sincromisor-frontend`）: PASS。3 files / 31 tests。
- `npm run gate`（cwd: implementation worktree root）: PASS。lint / build / test 全 step PASS。build は既存 chunk size warning のみ。

# Offerレジストリ試験の分割と残存コメントの是正

## 背景 / 目的

`internal/signaling/offer/registry_test.go` は物理668行、import・コメント・空行を除いて約613行あり、同時処理、容量、期限、終了、panic、ログとすべての試験補助が一つに集まっている。親パッケージの`operations_test.go`にも英語だけの説明が残る。Offer責務のパッケージ配置は維持し、試験対象をファイル名から追える状態へ整える。

## 完了条件（受け入れ条件）

- [ ] `registry_test.go`を再送・競合、容量・期限回収、終了・panic・ログ、共通補助の観点へ分け、各試験ファイルを規約上300行以下にする。
- [ ] 試験は`internal/signaling/offer`の同じパッケージに残し、非公開状態の検証のために本番要素を公開しない。
- [ ] `internal/signaling/operations_test.go`の英語だけの説明を、ログ境界で拒否・panic経路を確認する理由が分かる日本語へ改める。
- [ ] 同一要求の一本化、競合、2分の保持期限、1000件上限、終了済み記録、所有処理の取消・待合せという既存検証を削除しない。
- [ ] 変更理解範囲のコメント点検が完了している。

## 設計判断

本番の`Registry`と時刻境界は変更しない。試験補助は重複させず、同じ試験パッケージ内の専用ファイルに置く。

## スコープ境界

対象は`internal/signaling/offer/registry_test.go`と直接の試験補助、および`internal/signaling/operations_test.go`の残存コメントである。本番コード、HTTP形式、RTC処理は変更しない。

## 実装方針

既存の試験関数、偽時計、偽セッションサービスを責務ごとの試験ファイルへ移す。検証値や待機条件は変更しない。

## テスト

モジュールルートで`go test ./internal/signaling/...`と`go test -race ./internal/signaling/...`を実行する。

## ドキュメント同期の要否

公開契約とパッケージ責務は変わらないため、設計文書の同期は不要である。

# 実装時の補完

日本語短音声のGPU推論で、Tritonが実行時にC拡張を生成しようとして
`Failed to find C compiler` で失敗した。単純なGPU演算と死活確認だけでは検出できなかった。
実行に不可欠な依存としてDockerfileの実行段階へ `gcc` と `python3-dev` を追加した。
コンパイラー追加後に `Python.h` の不足も再現したため、Python開発用ヘッダーを含めた。
両方の導入後、同じ音声の認識が成功した。依存宣言・ロック・認識モデルの変更は不要だった。
